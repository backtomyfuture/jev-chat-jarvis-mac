# 隐私与数据说明

jev-jarvis 的核心原则是**纯只读、零封号风险**：不注入微信进程、不 hook、不解密本地数据库、不修改剪贴板、绝不替用户自动发送任何消息。

本项目现已全面升级为**全云端轻量化架构**，不在本地下载或运行任何庞大的深度学习模型（告别旧版 ~3.8 GB 的本地模型下载与显存/内存占用），判断与生成两层均通过加密 API 请求云端专业模型。

本页明确告知你：**哪些数据完全留在你的电脑本地、哪些数据会发往云端、发到哪里**。

---

## 完全留在你本地的部分

- **屏幕抓图与 OCR 识别**：
  抓图仅在 macOS 内存中由系统原生 Apple Vision 框架即时完成文本提取。**图片仅在内存中处理，绝不保存到磁盘，绝不上传图片至任何云端服务器**。
- **前台隐私守卫与自动遮蔽**：
  悬浮窗仅在微信位于前台时显示；微信离开前台、切换到其他应用或最小化时，面板立即清空当前消息气泡并自动退入后台隐藏，**绝不把私人聊天内容残留在屏幕上**，保护投屏与多任务环境下的屏幕隐私。
- **运行日志（`~/Library/Logs/jev-jarvis.log`）**：
  **刻意不记录任何微信消息正文与候选回复内容**，仅记录分阶段耗时（读屏/研判/生成/端到端）与脱敏状态，你可以放心公开贴进 GitHub Issue 排查问题。
- **API 密钥与配置**：
  保存在本地 `~/.config/jev-jarvis/env`，文件权限强制设为 `600`（仅当前用户拥有读写权限），绝不外发上传。

---

## 发往云端的数据流向说明

悬浮窗要对消息做出「诉求/时效/建议」研判并生成「候选回复」，必须将提取到的文字发往云端 API。数据流向分为两层：

### 1. 判断层（诉求、时效与建议）

- **传输内容**：仅提取出的当前消息文字及最近两轮对话纯文本上下文（用于综合上下文研判）。
- **传输路径**：通过 HTTPS 安全加密直连至你配置的 **TypeSafe Jev API** 端点（默认为官方 `https://api.typesafe.ai`，或你自己配置的 OpenRouter、Vercel AI Gateway 等兼容网关）。
- **处理方式**：TypeSafe Jev 是专门的结构化决策 System One 模型，只输出离散标签的校准概率（诉求、时效、建议），不产生生成式自由文本，不保存也不用于模型训练。个人聊天辅助，不做安全分类。

### 2. 生成层（候选回复生成）

- **传输内容**：当前待回复的消息文字、对话上下文，以及判断层给出的「应对建议」。
- **传输路径（两档选择）**：
  - **方式一（推荐）：配置你自己的 API Key**
    在设置中填入你自己申请的 `OPENAI_API_KEY`（如 DeepSeek、智谱、StepFun、MiniMax 等）或 `ANTHROPIC_API_KEY`。此时聊天文字**直接由你的 Mac 通过 HTTPS 发送给你自己选择的模型服务商**，不经过任何第三方服务器中转。
  - **方式二（零配置）：内置免费通道**
    未配置生成层 Key 时，应用使用打包内置的共享 Key 走作者维护的中转服务调用主流模型：
    - 仅用于即时生成回复候选并原样返回；
    - 不与你的设备身份、微信号或个人信息关联；
    - 绝不出售、绝不挪作他用；
    - 对商业保密或敏感场景，强烈建议使用方式一配置个人专属 Key。

---

## 使用与免责

- 候选回复由云端大模型根据研判策略自动生成，仅供参考；是否采纳、填入或发送完全由你手动决定，本项目不对生成内容的准确性与社交得体性承担连带责任。
- 请遵守微信等各软件的许可协议与当地法律法规，严禁利用本项目从事任何违法违规行为。
